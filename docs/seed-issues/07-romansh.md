# Romansh (rm) strings for layer 0 and the report
Labels: good first issue,i18n/en

Add `i18n/rm.json` (Rumantsch Grischun) mirroring `en.json`, extend `Lang`/`LANGS`, the canton map (GR → still de by default; rm on `--lang rm`), the report language switcher and the tests. Native review required (docs/i18n-review.md).
