# Report UI: print layout polish and PDF check on A4
Labels: report-ui,good first issue

Print the golden example (`examples/zurich-badenerstrasse/index.html`) from Chrome and Firefox to A4: verify page breaks inside cards, the sources table, page numbers and that links print their URL. Fix in `ui/local-report/report.css` (`@media print`).
